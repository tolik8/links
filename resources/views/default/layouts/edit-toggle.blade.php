@if ($d_none === 'd-none')
    <i id="edit-toggle" class="fas fa-toggle-off cursor-pointer" show="off"></i>
@else
    <i id="edit-toggle" class="fas fa-toggle-on cursor-pointer" show="on"></i>
@endif
